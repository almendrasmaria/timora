# PostgreSQL
POSTGRES_DB=timora
POSTGRES_USER=timora
POSTGRES_PASSWORD=timora_dev
POSTGRES_PORT=5433

# Backend
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5433/timora
SPRING_DATASOURCE_USERNAME=timora
SPRING_DATASOURCE_PASSWORD=timora_dev
JWT_SECRET=change-me-in-production-use-a-long-random-string
JWT_EXPIRATION_MS=86400000
CORS_ALLOWED_ORIGINS=http://localhost:4200
FRONTEND_BASE_URL=http://localhost:4200

# Email (forgot password) — SMTP credentials, e.g. Gmail app password, Resend, SendGrid, etc.
MAIL_HOST=
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM=no-reply@timora.app

# Frontend
API_URL=http://localhost:8080/api
